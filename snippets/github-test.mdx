import { githubLookup } from "/snippets/github.js";


export const GitHubExtractorViaFunc = async ({
  owner,
  repo,
  path,
  startMarker,
  endMarker,
  branch = 'main',
  language = 'python',
}) => {
  return githubLookup(owner, repo, path, startMarker, endMarker, branch, language)
};
